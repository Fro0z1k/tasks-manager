<div class="nav-container">
  <nav class="nav-1">
    <div class="container">
      <div class="row">
        <div class="col-xs-12">
          <ul class="menu">
            <a href="/" class="home-link">
              <%= image_tag('tm-logo.png', class: "logo" ) %>
            </a>
            <% unless request[:controller] == 'home' %>
              <li><%= link_to "Dashboard", dashboard_index_path %></li>
              <% if request[:controller] == 'dashboard' %>
                <li><%= link_to 'create Project', new_project_path, remote: true %></li>
              <% elsif request[:controller] == 'projects' && request[:action] == 'show' || request[:action] == 'create' || request[:action] == 'update' %>
                <li><%= link_to "Edit: '#{ @project.name }'", edit_project_path(@project), remote: true  %></li>
                <li><%= link_to "Remove: '#{ @project.name }'", project_path(@project), method: :delete,
                                            data: { confirm: "Are you sure you want to delete '#{@project.name}'?" } %></li>
              <% end %>
            <% end %>
          </ul>

          <ul class="social-links">
            <% if current_user %>
              <%= current_user.email %>
              <%= link_to "Log out", destroy_user_session_path, :method => :delete %>
            <% end %>
          </ul>
        </div>
      </div><!--end of row-->
    </div><!--end of container-->
    <div class="mobile-toggle">
      <div class="bar-1"></div>
      <div class="bar-2"></div>
    </div>
  </nav>
</div>